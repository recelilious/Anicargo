# Repository Scripts

Current scripts:

- `reset-backend-runtime.ps1`: clear local runtime data for cold-start testing
- `decode-backend-log.py`: expand compact backend log lines into readable text
- `test-downloader-service.ps1`: basic standalone downloader smoke test
- `test-downloader-service-longrun.ps1`: longer-running downloader queue and throttling test
